import { DisallowSpaceDirective } from './disallow-space.directive';

describe('DisallowSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new DisallowSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
